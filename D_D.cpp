#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
    int t;
    cin>>t;
    while(t--){
        ll p,q;
        cin>>p>>q;
        ll gcd =__gcd(p,q);
        if(gcd==1)cout<<max(p,q)<<endl;
        else cout<<
    }
    return 0;
}